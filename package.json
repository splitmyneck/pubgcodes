{
  "name": "pubg-codes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i input.css -o dist/output.css --minify",
    "build:dev": "tailwindcss -i input.css -o dist/output.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
